import axios from "axios";

const API_URL = 'http://localhost:8080/api/user/';

class UserService{
    // TODO: add all GET methods for user information
    // TODO: split UserService into StudentService and TeacherService, 

    /*
    example usage: 
    
    getGameCard(){
        return axios.get(API_URL + "game_card", {headers: authHeader()})
    }
    */
}

export default new UserService();